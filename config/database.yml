    default: &default
      adapter: postgresql
      encoding: unicode
      pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
      username: postgres
      passwprd: 1111
      host: localhost
      port: 5432

      development:
        <<: *default
        database: cat_shop_development

        test:
          <<: *default
          database: cat_shop_test
